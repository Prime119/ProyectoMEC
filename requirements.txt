# ============================================================
#  ProyectoMEC — Dependencias
# ============================================================

# --- Consola Industrial (existente) ---
numpy>=1.24
pandas>=2.0
PyQt6>=6.5
pyqtgraph>=0.13
pyserial>=3.5
openpyxl>=3.1
tensorflow>=2.15
keras>=3.0
scikit-learn>=1.3

# --- Asistente MEC (cerebro LLM + utilidades) ---
httpx>=0.27            # cliente HTTP para Ollama (cerebro LLM local)
psutil>=5.9            # detección de hardware (RAM/CPU)

# --- Voz de MEC (STT + TTS, ambos offline) ---
faster-whisper>=1.0    # oído / STT offline (transcripción de voz a texto)
sounddevice>=0.4       # captura y reproducción de audio
piper-tts>=1.2         # voz / TTS offline (text-to-speech en español)


# --- Palantir CFE: Visión Satelital + Detección IA ---
pillow>=10.0           # composición y manejo de imágenes satelitales
# httpx ya listado arriba — también se usa para descargar teselas satelitales

# --- Detección IA (opcional — para modelo entrenado propio) ---
# Descomenta si vas a usar un modelo YOLOv8/ONNX entrenado:
# onnxruntime>=1.16    # inferencia del modelo (usa onnxruntime-gpu si tienes CUDA)
# ultralytics>=8.0     # solo si quieres entrenar/exportar tu propio modelo YOLOv8


# --- Palantir CFE: Visualización 3D holográfica (gemelo digital) ---
PyOpenGL>=3.1          # motor 3D para el visor holográfico (pyqtgraph.opengl)
PyOpenGL_accelerate>=3.1  # opcional: acelera el render 3D


# --- Palantir CFE: Mapa OSINT geográfico (Leaflet en QWebEngineView) ---
PyQt6-WebEngine>=6.5   # navegador embebido para el mapa Leaflet estilo OSINT
# Requiere internet para las teselas del mapa y la librería Leaflet (CDN).


# --- Palantir CFE: Versión Web (recomendada — se abre en el navegador) ---
aiohttp>=3.9           # servidor web async + WebSocket para datos en tiempo real


# --- Detección IA en FALCON web (para usar tu modelo entrenado) ---
onnxruntime>=1.16      # inferencia del modelo .onnx entrenado
# pillow ya listado arriba (imágenes satelitales)
